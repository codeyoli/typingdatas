# create an empty repository at project folder root
git init
git init
git init

# if you have not prepared the project for the team
# clone the shared project provided by others
git clone
git clone
git clone

# see changes in Working Directory and Staging Area
git status
git status
git status

# add desired changes from Working Directory to Staging Area
git add data.json
git add Demo.java
git add pom.xml

# make a snapshot or backup or project
git commit
git commit -m "message"

# see the snapshots in the project
git log
git log --oneline
git log --graph
git log -n5