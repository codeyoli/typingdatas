# configure a username
git config --global user.name "name"
git config --global user.name "name"
git config --global user.name "name"
git config --global user.name "name"

# configure an email
git config --global user.email "you@gmail.com"
git config --global user.email "you@gmail.com"
git config --global user.email "you@gmail.com"
git config --global user.email "you@gmail.com"

# create a repository in the project root directory
git init
git init
git init
git init
git init
git init

# see the changes listed in the Working Directory
git status
git status
git status
git status -s
git status -s
git status -s
git status -s

# move the changes from Working Directory to Staging Area
# note that the listed file name represents the changes
git add Demo.java
git add Demo.java
git add Demo.java
git add Demo.java
git add Demo.java
git add Demo.java

# move all Java file changes from Working Directory to the Staging Area
git add *.java
git add *.java
git add *.java
git add *.java

# move all the changes from Working Directory to the Staging Area
git add --all
git add --all
git add --all
git add --all

# take a snapshot of a project with all the changes in the Staging Area
git commit
git commit
git commit
git commit
git commit

# take a snapshot of a project with a commit message
git commit -m "message here"
git commit -m "message here"
git commit -m "message here"
git commit -m "message here"

# see all the commits made in the project
git log
git log
git log
git log
git log

# see all the commits made with a short one-line detail
git log --oneline
git log --oneline
git log --oneline
git log --oneline
git log --oneline
git log --oneline

# see all the commits made with the graph view
git log --graph
git log --graph
git log --graph
git log --graph
git log --graph

DONE